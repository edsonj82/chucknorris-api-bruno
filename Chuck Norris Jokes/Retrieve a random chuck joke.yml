info:
  name: Retrieve a random chuck joke
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseURL}}/jokes/random"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
