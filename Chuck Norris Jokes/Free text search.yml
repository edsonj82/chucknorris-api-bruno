info:
  name: Free text search
  type: http
  seq: 6

http:
  method: get
  url: "{{baseURL}}/jokes/search?query=query"
  headers:
    - name: accept
      value: application/json
  params:
    - name: query
      value: query
      type: query
  auth: inherit

settings:
  encodeUrl: false
  timeout: 0
  followRedirects: true
  maxRedirects: 5
